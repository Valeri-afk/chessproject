#pragma once

#include "ui_framework/core/layoutmanager.hpp"

namespace ui
{
    // Transitional internal alias. The implementation is migrated to the
    // final class name after all include sites are switched.
    using LayoutSystem = LayoutManager;
}
