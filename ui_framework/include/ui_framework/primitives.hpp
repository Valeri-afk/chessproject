#pragma once

// Transitional public entry point. The implementation header keeps its
// existing declarations until the private/public source split is completed.
#include "ui_framework/core/primitives.hpp"
